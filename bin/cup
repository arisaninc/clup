#!/usr/bin/env node

var commander = require('commander');

commander
  .command('init <db-url>',
           'initialize clio-up with IaaS credentials from database')
  .command('deploy <db-url>',
           'deploy clio to IaaS')
  .parse(process.argv);
